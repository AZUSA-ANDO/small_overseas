
<div style="height: 600px", class="d-flex align-items-center">
  <div class="container">
    <div class="row mt-5">
      <div class="mx-auto">
        <% if @user.id == 99 %>
          <h4 class="text-center">このページでは何も操作できません</h4>
        <% else %>
          <h4 class="text-center">本当に退会しますか？</h4></br>
          <p class="text-center">
            退会するとログインすることが出来なくなり</br>DMのやり取りやいいね、コメント等ができなくなります。</br>
            投稿は消えないので削除したい投稿があれば</br>退会する前に削除してください。</br>
          </p>
          <p class="text-center">
            退会する場合は「はい」をクリックしてください。
          </p>
        </div>
      </div>

      <div class="row mt-4">
        <div class="mx-auto">
          <%= link_to "いいえ", "/users/#{current_user.id}", class:'btn btn-sm btn-secondary' %>
          <%= link_to "はい", quit_users_path, method: :PATCH, data: {confirm: "本当に退会しますか？"}, class:'btn btn-sm btn-outline-secondary ml-4' %>
        </div>
      </div>
        <% end %>
  </div>
</div>